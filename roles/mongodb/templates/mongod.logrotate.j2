{{ log_dir }}/*.log {
    create 0600 mongod mongod
    daily
    rotate 10
    missingok
    notifempty
    compress
    delaycompress
    dateext
    sharedscripts
    postrotate
        killall -SIGUSR1 mongod > /dev/null 2>&1 || true
    endscript
}